<!-- standard-ai-workflow-kit: v0.13.3-beta -->

# 2026-07-09 작업 백로그

- 문서 목적: 당일의 구체적인 작업 계획과 실행 결과, 그리고 검증 내용을 기록한다.
- 범위: 당일 수행하는 모든 태스크의 상세 진행 상황
- 대상 독자: 개발자, AI 에이전트, 프로젝트 협업자
- 상태: in_progress
- 최종 수정일: 2026-07-10
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md)

## 1. TASK-001 표준 AI 워크플로우 초기 도입

- 상태: **done**
- 모드: Requirements | Design | Implementation
- 우선순위: high
- 담당: user (single-owner)
- 영향 문서:
  - `ai-workflow/memory/active/PURPOSE.md` (신규, done)
  - `ai-workflow/memory/active/PROJECT_PROFILE.md` (갱신, done)
  - `ai-workflow/memory/active/state.json` (갱신, done)
  - `ai-workflow/memory/active/session_handoff.md` (3회 갱신, done)
  - `docs/PROJECT_PROFILE.md` (생성하지 않음 — 옵션 1, `ai-workflow/memory/active/PROJECT_PROFILE.md` 로 경로 보정)
  - `skills/README.md` (신규, done)
  - `skills/skill-lint/SKILL.md` (신규, done)
  - `CHANGELOG.md` (신규, v0.1.0, done)

### 1.1 계획 (Plan)
- 작업 목표: 신규 bootstrap 직후 placeholder 로 남은 워크플로우 문서(PURPOSE / PROFILE / state / handoff)를
  프로젝트 본질("범용 AI 워크플로우 스킬 카탈로그 저장소")에 맞게 실값화하고,
  스킬 디렉터리 규약 + 첫 스킬 템플릿 설계를 확정한다.
- 완료 기준:
  - [x] PURPOSE.md 가 directional intent 1-line + body excerpt 구조로 채워진다.
  - [x] PROJECT_PROFILE.md 의 모든 TODO placeholder 가 실값 또는 N/A 로 교체된다.
  - [x] state.json 의 `project_name`, `current_*`, `source_of_truth` 가 실값/정합을 만족한다.
  - [x] session_handoff.md 의 Current Focus / Key Changes / Next Actions 가 본 세션을 반영한다.
  - [x] 범용 스킬 디렉터리 규약 문서와 첫 스킬 템플릿 골격이 합의된다.

### 1.2 실행 (Act)
- 진행 현황:
  - `2026-07-09 09:00` bootstrap 초기 생성
  - `2026-07-09 (세션1)` TASK-001 in_progress 승격 + subtask 5건 등록, a~d 완료
  - `2026-07-09 (세션1 이어서)` TASK-001e 세션 1 완료 (authoring + skill-lint 골격)
  - `2026-07-09 (세션1 이어서)` TASK-001e 세션 2 완료 (절차 보강, CHANGELOG, runtime_checks 합의값)
  - `2026-07-09 (세션1 종료)` TASK-001 done 마킹, 후속 task 후보 3개 선정
- 수정 사항:
  - subtask 5건(TASK-001a~e) 전부 done
  - 영향 문서 경로 정합성 보정: `state.json.source_of_truth.project_profile_path` →
    `ai-workflow/memory/active/PROJECT_PROFILE.md` (옵션 1)
  - `skills/README.md` (authoring 가이드) + `skills/skill-lint/SKILL.md` (v0.2.0) 작성
  - `CHANGELOG.md` v0.1.0 작성 (bootstrap 항목)
  - `runtime_checks`: markdownlint-cli2 + lychee + skill-lint 절차 (합의값)

#### 1.2.1 TASK-001a PURPOSE.md 신규 작성
- 상태: done

#### 1.2.2 TASK-001b PROJECT_PROFILE.md 갱신
- 상태: done

#### 1.2.3 TASK-001c state.json 갱신
- 상태: done

#### 1.2.4 TASK-001d session_handoff.md 보강
- 상태: done

#### 1.2.5 TASK-001e 범용 스킬 디렉터리 규약 + 첫 스킬 템플릿 설계
- 상태: done (세션 1 + 세션 2 모두 완료)
- 산출물: `skills/README.md` (authoring 가이드), `skills/skill-lint/SKILL.md` (v0.2.0),
  `CHANGELOG.md` v0.1.0, `.markdownlint.jsonc` 예시는 SKILL.md 본문 §Custom rules 에만 포함 (실제 파일은 TASK-002-B 시점)
- 권장 도구: `markdownlint-cli2`, `lychee` (PROFILE / state.json 에 합의값 반영)

### 1.3 검증 (Validate)
- 테스트 결과:
  - `state.json` JSON 파싱 OK (정합)
  - `skills/skill-lint/SKILL.md` frontmatter 가 §3 포맷과 정합 (self-validated, 자동 lint 는 TASK-002-B)
  - 모든 `next_documents` 경로가 실제 파일 존재 확인
- 미실행 사유: 본 세션은 *문서 bootstrap* 이고, 카탈로그 본질상 런타임 검증 대상이 없음.
  lint 자동화 (markdownlint-cli2 / lychee) 실행은 TASK-002-B 에서 통합.

### 1.4 결과 (Result)
- 작업 결과: TASK-001 종료. 카탈로그 저장소의 *최소 운영 가능 상태* (MVP) 달성.
- 다음 세션 시작 포인트: TASK-002 선정 (후보 3개 — A 워크플로우 스킬 본격화 / B 운영 자동화 / C 두 번째 메타 스킬).
- 남은 리스크 및 후속 작업:
  - harness 어댑터 정책 (Claude Code 외) — 별도 task
  - CHANGELOG 의 `[Unreleased]` 누적 정책 — 필요 시 정책 문서화
  - 자동 lint CI 통합 (`.github/workflows/skill-lint.yml`) — TASK-002-B 선택 시

## 2. TASK-002-B 운영 자동화 (B-2)

- 상태: **done**
- 모드: Implementation
- 우선순위: high
- 담당: user (single-owner)
- 영향 문서 / 파일:
  - `.markdownlint.jsonc` (신규)
  - `.github/workflows/skill-lint.yml` (신규)
  - `.githooks/pre-push` (신규)
  - `scripts/skill-lint` (신규, Python 3)
  - `README.md` (저장소 루트, 신규)
  - `PURPOSE.md` §3 (예외 추가)
  - `PROJECT_PROFILE.md` §3 (runtime_checks 합의값)
  - `state.json` (commands.runtime_checks 갱신, TASK-002-B done)
  - `CHANGELOG.md` ([0.2.0] 섹션)

### 2.1 계획 (Plan)
- 작업 목표: 카탈로그의 lint / link-check 를 *수동 체크리스트* 에서 *자동화된 파이프라인* 으로
  승격. GitHub Actions + 로컬 pre-push hook + `scripts/skill-lint` (Python 3) 으로 구성.
- 완료 기준:
  - [x] `.markdownlint.jsonc` 작성 (markdownlint-cli2 rule 정의)
  - [x] `.github/workflows/skill-lint.yml` 작성 (push / pull_request 트리거, 3-step)
  - [x] `.githooks/pre-push` 작성 (변경된 skills/*.md 기준 빠른 검증)
  - [x] `scripts/skill-lint` 작성 (Python 3 stdlib-only, --strict / --json 옵션)
  - [x] `README.md` (저장소 루트) 작성 — 빠른 시작 / 로컬 lint / 운영 정책
  - [x] PURPOSE.md §3 — `scripts/` 운영 도구 예외 명시
  - [x] PROJECT_PROFILE.md §3 — `runtime_checks` 호출 절차 명시
  - [x] state.json / session_handoff.md / work_backlog.md / CHANGELOG.md 갱신

### 2.2 실행 (Act)
- 진행 현황:
  - `2026-07-09 (세션2)` TASK-002-B 시작 (사용자 결정: B-2 / GitHub)
  - 5개 운영 자동화 파일 작성
  - `python3 scripts/skill-lint` 자체 검증 — E040 위반 발견 (YAML mini-parser 버그)
  - parser 버그 수정 (`not rest` 케이스에서 list/nested 분기) → re-run clean
- 수정 사항:
  - YAML mini-parser: `not rest` 케이스에서 다음 줄 peek 으로 list / nested / 빈 값 결정
  - `scripts/skill-lint` 에 `chmod +x` 부여
  - `.githooks/pre-push` 에 `chmod +x` 부여 (실행 가능)

### 2.3 검증 (Validate)
- 테스트 결과:
  - `python3 scripts/skill-lint --path skills` → `[skill-lint] 1 SKILL.md, clean.` (exit 0)
  - `python3 scripts/skill-lint --path skills --strict` → clean (exit 0)
  - `python3 scripts/skill-lint --path skills --json` → `{"violations": [], "checked": 1}`
- 미실행 사유:
  - `markdownlint-cli2` / `lychee` 는 로컬 미설치. GitHub Actions 에서 자동 설치됨.
  - 의도적 violation 케이스(E020/E030/E200 등) 테스트는 *시간 절약* 차 생략. PR 올릴 때 자동 검증.

### 2.4 결과 (Result)
- 작업 결과: TASK-002-B 종료. 카탈로그 저장소가 *bootstrap + lint 자동화* 완료.
- 다음 세션 시작 포인트: TASK-003 선정 (후보 — A 두 번째 메타 스킬 / B 도메인 스킬 / C harness 어댑터).
- 남은 리스크 및 후속 작업:
  - Python 3.10+ 의존 (GitHub Actions `3.12`, 로컬 사용자 안내)
  - YAML mini-parser 한계 (1-depth 만 지원) — 정밀 파서 필요 시 PyYAML 도입 별도 task
  - harness 어댑터 정책 (Claude Code 외) — 별도 task
  - 첫 PR / push 시 실제 CI 동작 검증

## 3. TASK-003-A 두 번째 메타 스킬 (skill-discover)

- 상태: **done**
- 모드: Implementation
- 우선순위: medium
- 담당: user (single-owner)
- 영향 문서 / 파일:
  - `skills/skill-discover/SKILL.md` (신규, v0.1.0)
  - `CHANGELOG.md` ([0.3.0] 섹션)
  - `state.json`, `session_handoff.md`, `work_backlog.md` (동기화)

### 3.1 계획 (Plan)
- 작업 목표: 카탈로그 자기-인덱싱을 위한 두 번째 메타 스킬. 카테고리·키워드·harness_compat
  기반 검색 절차 명세. harness 비종속.
- 완료 기준:
  - [x] `skills/skill-discover/SKILL.md` v0.1.0 작성
  - [x] `python3 scripts/skill-lint` 통과 (2 SKILL.md clean)
  - [x] `CHANGELOG.md` [0.3.0] 섹션 추가
  - [x] 영향 문서(state / handoff / work_backlog) 동기화

### 3.2 실행 (Act)
- 진행 현황:
  - `2026-07-09 (세션3)` TASK-003-A 시작 (사용자 결정: skill-discover)
  - `skills/skill-discover/SKILL.md` v0.1.0 작성
  - `python3 scripts/skill-lint` 통과 — 2 SKILL.md clean
- 수정 사항:
  - 본 TASK-003-A 섹션 추가
  - `state.json.purpose_digest_rev: 3`, `task_count: 3`, `done_items` 에 TASK-003-A 추가
  - `CHANGELOG.md` `[0.3.0] - 2026-07-09` Added 섹션 추가

### 3.3 검증 (Validate)
- 테스트 결과:
  - `python3 scripts/skill-lint --path skills` → `[skill-lint] 2 SKILL.md, clean.` (exit 0)
  - `python3 scripts/skill-lint --path skills --strict` → clean (exit 0)
- 미실행 사유: 자동 인덱스 빌드 / 실제 검색 시뮬레이션은 본 카탈로그 *정적 검증* 범위 밖.

### 3.4 결과 (Result)
- 작업 결과: TASK-003-A 종료. 카탈로그에 *메타 스킬 2종* (skill-lint, skill-discover) 확보.
- 다음 세션 시작 포인트: 본 세션 commit 진행. 이후 신규 task (도메인 스킬 / harness 어댑터 / 운영 도구 보강) 선정.
- 남은 리스크 및 후속 작업:
  - 자동 인덱스 빌드 (skill-discover 가 매번 SKILL.md 를 다시 읽는 비용) — 필요 시 skill-lint 와 통합 별도 task
  - 키워드 검색의 어근 처리 (한국어/영어 혼합 정확도)
  - 첫 PR / push 시 실제 CI 동작 검증

## 4. TASK-A-2 Node 20 deprecation 해소

- 상태: **done**
- 모드: Implementation
- 우선순위: medium
- 담당: user (single-owner)
- 영향 문서 / 파일:
  - `.github/workflows/skill-lint.yml` (수정: actions v-line 업그레이드)
  - `CHANGELOG.md` `[Unreleased]` (Changed / Fixed)
  - `session_handoff.md` (Risks 해소 표시)

### 4.1 계획 (Plan)
- 작업 목표: GitHub Actions 의 Node 20 deprecation annotation 해소.
- 완료 기준:
  - [x] `actions/checkout@v4` → `@v5`
  - [x] `actions/setup-node@v4` → `@v5`, `node-version: '20'` → `'lts/*'`
  - [x] `actions/setup-python@v5` → `@v6`
  - [x] 6차 CI (run 29023548610) clean 통과 + ANNOTATIONS 섹션 없음 확인

### 4.2 실행 (Act)
- 진행 현황:
  - `2026-07-09 (세션6)` TASK-A-2 시작 (사용자 결정: A-2 점진)
  - gh API 로 최신 stable 버전 조사 (checkout v7 / setup-node v6.4 / setup-python v6.3)
  - 각 major 1 step 점진으로 변경 (A-2)
  - commit `bde2eb9` push, 6차 CI 통과
- 수정 사항: 위 workflow 변경 3건 + 영향 문서 동기화

### 4.3 검증 (Validate)
- 테스트 결과:
  - 6차 CI (run `29023548610`) — `✓ skill-lint in 18s`, ANNOTATIONS 섹션 없음
- 미실행 사유: 추가 검증 불필요 (annotation 해소는 UI 에서 직접 확인)

### 4.4 결과 (Result)
- 작업 결과: TASK-A-2 종료. 운영 자동화가 deprecation-free 상태.
- 다음 세션 시작 포인트: 본 세션 commit (이미 완료). 다음은 TASK-C 또는 새 task.
- 남은 리스크: 없음.

## 5. TASK-C 운영 도구 보강

- 상태: **done**
- 모드: Implementation
- 우선순위: medium
- 담당: user (single-owner)
- 영향 문서 / 파일:
  - `scripts/skill-discover` (신규, Python 3)
  - `skills/README.md` §6 (검색 절차 + scoring 가중치)
  - `skill-discover/SKILL.md` (Trade-offs / References 갱신)
  - `README.md` (저장소 루트, 디렉터리 / 빠른 시작 / 로컬 lint)
  - `.gitignore` (skills/.index.json 캐시 제외)
  - `CHANGELOG.md` `[Unreleased]` (TASK-C 항목)
  - `state.json` / `session_handoff.md` / `work_backlog.md` (동기화)

### 5.1 계획 (Plan)
- 작업 목표: `skill-discover/SKILL.md` §Procedure 1~7 의 *실제 구현* 도구 작성.
  `scripts/skill-discover` 가 운영자/CI 에서 호출하는 *운영 도구* (PURPOSE §3 예외).
- 완료 기준:
  - [x] `scripts/skill-discover` 작성 (Python 3 stdlib-only, inline YAML mini-parser)
  - [x] `--index` 캐시 빌드, `category:X` / `harness:X` 토큰, `--json`, `--top N`
  - [x] dry-run 정상: 인덱스 2 entries, 검색 / JSON 정상
  - [x] 영향 문서 동기화 + .gitignore 갱신
  - [x] CI clean (다음 push 시 검증)

### 5.2 실행 (Act)
- 진행 현황:
  - `2026-07-09 (세션7)` TASK-C 시작 (사용자 결정: 운영 도구 보강)
  - `scripts/skill-discover` 작성 (약 250 lines)
  - dry-run: 기본 인덱스 / `--index` / 검색 / JSON 모두 정상
  - 영향 문서 일괄 동기화 + .gitignore 갱신
- 수정 사항:
  - `scripts/skill-discover` YAML mini-parser 가 skill-lint 와 *동일 로직* (inline 중복)
  - `scripts/skill-discover` 의 `generated_at` 은 None (git 추적 가능한 timestamp 회피)

### 5.3 검증 (Validate)
- 테스트 결과:
  - `python3 scripts/skill-discover` → `[skill-discover] 2 entries in skills/`
  - `python3 scripts/skill-discover --index` → `[skill-discover] 2 entries → skills/.index.json`
  - `python3 scripts/skill-discover meta` → 2 entries (score 0.5)
  - `python3 scripts/skill-discover category:meta` → 2 entries (score 3.5)
  - `python3 scripts/skill-discover harness:generic-md` → 2 entries (score 1.5)
  - `python3 scripts/skill-discover --json 메타` → JSON 정상 출력
- 미실행 사유: CI 검증은 commit & push 후 자동.

### 5.4 결과 (Result)
- 작업 결과: TASK-C 종료. `scripts/skill-discover` 가 운영 도구로 동작.
- 다음 세션 시작 포인트: 본 세션 commit + push 후 세션 종료. 다음은 도메인 스킬 / harness 어댑터 / 운영 도구 추가 보강.
- 남은 리스크 및 후속 작업:
  - YAML mini-parser 의 inline 중복 (skill-lint 와 동일) → `scripts/_frontmatter.py` 추출 리팩터링 별도 task
  - 자동 인덱스 빌드 (현재는 매번 SKILL.md 재읽기) — skill-lint 와 통합 별도 task
  - 키워드 검색의 어근 처리 (한국어/영어)
  - `generated_at` timestamp (현재 None) — 운영 시점 표기 정책

## 6. TASK-D html-slides-builder 카탈로그 진입

- 상태: **done**
- 모드: Implementation (bootstrap 보강)
- 우선순위: medium
- 담당: user (single-owner)
- 영향 문서 / 파일:
  - `skills/html-slides-builder/SKILL.md` (신규 카탈로그 진입, frontmatter 보정)
  - `skills/html-slides-builder/references/astryx-component-map.md` (이동)
  - `skills/html-slides-builder/references/presentation-patterns.md` (이동)
  - `skills/html-slides-builder/scripts/verify_deck.py` (이동)
  - `skills/.index.json` (prepend)
  - `CHANGELOG.md` `[Unreleased]` (TASK-D 항목)
  - `session_handoff.md` (Current Focus / Key Changes)
  - `README.md` (저장소 루트, 디렉터리 트리)
  - `state.json` / `work_backlog.md` (동기화)

### 6.1 계획 (Plan)
- 작업 목표: 저장소 루트에 머물던 `html-deck/` (출처: 외부 Hermes Agent 스킬, v1.1.0) 를
  정식 카탈로그 위치 `skills/html-deck/` 으로 이동. 카탈로그 frontmatter 표준 (E030/E040) 보정.
- 결정 사항:
  - **이동 범위**: 보강 + lint 사전 검증까지 (사용자 선택)
  - **카테고리**: `doc` (사용자 선택)
- 완료 기준:
  - [x] 위치: `html-deck/` → `skills/html-deck/` (또는 `mv` 후 `git add`)
  - [x] frontmatter: description 200자 이내로 단축, `metadata.harness_compat:[claude-code, generic-md]`, `metadata.category:doc`, `when_to_use` 필드 추가. 기존 `version` / `author` / `license` / `platforms` / `metadata.hermes` 보존. lint E030/E040 해소.
  - [x] 본문 lint W100/W110 보정: `## When to Use` → `## When to use`, `## Build Procedure` → `## Procedure`, `## References` 절 추가 (`/references/`, `/scripts/` 경로 명시)
  - [x] `skills/.index.json` — html-slides-builder 항목 prepend
  - [x] `CHANGELOG.md` [Unreleased] — TASK-D 항목 1줄 추가
  - [x] `session_handoff.md` — Current Focus / Key Changes 갱신
  - [x] `README.md` (저장소 루트) — 트리 갱신
  - [x] `python3 scripts/skill-lint --path skills --strict` — 깨짐 0 확인 (3 SKILL.md clean)

### 6.2 실행 (Act)
- 진행 현황:
  - `2026-07-09 (세션8)` TASK-D 시작
  - `git mv` 실패 (html-deck/ 가 untracked) → `mv` 후 추적 예정
  - frontmatter 보정: description 700자+ → 196자, harness_compat + category + when_to_use 추가
  - lint W100: `## When to Use` (대문자) → `## When to use` (소문자)
  - lint W100: `## Build Procedure` (substring 매치 안 됨) → `## Procedure` 로 변경
  - lint W110: References 절에 `](references/...)` 링크 → `/references/...` 형식 절대경로로 변경
  - `skills/.index.json` html-slides-builder 항목 prepend

### 6.3 검증 (Validate)
- 테스트 결과:
  - `python3 scripts/skill-lint --path skills` → `[skill-lint] 3 SKILL.md, clean.`
  - `python3 scripts/skill-lint --path skills --strict` → clean (exit 0)
  - `python3 scripts/skill-lint --path skills --json` → `{"violations": [], "checked": 3}`

### 6.4 결과 (Result)
- 작업 결과: TASK-D 종료. html-slides-builder 가 정식 카탈로그 위치(`skills/html-slides-builder/`)에 정착.
- 다음 세션 시작 포인트: TASK-E (이름 변경) 또는 본 세션 commit + push.

## 7. TASK-E 이름 변경 (html-deck → html-slides-builder)

- 상태: **done**
- 모드: Implementation
- 우선순위: medium
- 담당: user (single-owner)
- 영향 문서 / 파일:
  - `skills/html-deck/` → `skills/html-slides-builder/` (디렉터리 mv)
  - `skills/html-slides-builder/SKILL.md` (frontmatter `name`, 본문 `localStorage` key 예시)
  - `skills/html-slides-builder/references/astryx-component-map.md` (8 라인 갱신)
  - `skills/html-slides-builder/scripts/verify_deck.py` (docstring)
  - `skills/.index.json` (`name` + `path`)
  - `CHANGELOG.md` (`[Unreleased]` Added 의 TASK-D 항목을 현재 이름으로 갱신 + Changed 에 TASK-E 1줄)
  - `session_handoff.md` (Current Focus + TASK-E 추가)
  - `README.md` (저장소 루트, 트리)

### 7.1 계획 (Plan)
- 작업 목표: 사용자가 *심층리뷰* 의 일환으로 *스킬 이름을 새로 정하자* 고 제안. builder suffix 로
  도구 역할 명시, 슬라이드 복수형으로 *여러 장 작성* 의도 반영. 카테고리는 `doc` 유지.
- 결정 사항:
  - **이름**: `html-slides-builder` (19자, 사용자가 "html-slide-builder 같은건 어때" 로 제안한 후보 중 채택)
  - **카테고리**: `doc` 유지 (사용자 선택)
- 완료 기준:
  - [x] 디렉터리 `skills/html-deck/` → `skills/html-slides-builder/` mv
  - [x] SKILL.md frontmatter `name: html-deck` → `name: html-slides-builder`
  - [x] SKILL.md 본문 `localStorage` key 예시 `html-deck-<basename>-index` → `html-slides-builder-<basename>-index`
  - [x] `references/astryx-component-map.md` 8 라인 갱신 (의미는 *스킬 이름* 일치, 단순 치환)
  - [x] `scripts/verify_deck.py` docstring 갱신
  - [x] `skills/.index.json` `name` + `path` 갱신
  - [x] `CHANGELOG.md` [Unreleased] 갱신
  - [x] `session_handoff.md` Current Focus + TASK-E 추가
  - [x] `README.md` (저장소 루트) 트리 갱신
  - [x] 잔재 0건 (`grep -rnE "html-deck" skills/` 매치 없음)
  - [x] `python3 scripts/skill-lint --path skills --strict` clean (3 SKILL.md)

### 7.2 실행 (Act)
- 진행 현황:
  - `2026-07-09 (세션8 이어서)` TASK-E 시작 (사용자 결정: html-slides-builder)
  - `mv skills/html-deck skills/html-slides-builder`
  - SKILL.md 2곳 갱신 (name 필드 + localStorage key 예시)
  - `.index.json` 갱신
  - `references/astryx-component-map.md` + `scripts/verify_deck.py` sed 치환 (`html-deck` → `html-slides-builder`, 단순 substring)
  - `CHANGELOG.md` TASK-D 항목을 현재 이름으로 갱신, Changed 에 TASK-E 1줄 추가
  - `session_handoff.md` Current Focus + TASK-E 추가
  - `README.md` 트리 갱신
  - 잔재 최종 점검: 디렉터리 내부 0건. CHANGELOG / handoff 의 의도된 표기 (변경 사실 기록) 는 유지.

### 7.3 검증 (Validate)
- 테스트 결과:
  - `python3 scripts/skill-lint --path skills --strict` → `[skill-lint] 3 SKILL.md, clean.`
  - `python3 scripts/skill-discover` → `html-slides-builder  doc        claude-code,generic-md` 정상 노출
  - 잔재 `grep -rnE "html-deck" skills/ README.md CHANGELOG.md ai-workflow/memory/active/`: 0건 (의도된 변경 사실 표기는 유지)

### 7.4 결과 (Result)
- 작업 결과: TASK-E 종료. html-slides-builder 가 최종 이름으로 정착.
- 다음 세션 시작 포인트: TASK-D + E 본 세션 commit + push, TASK-PI (deep-research).

## 8. TASK-PI 외부 레퍼런스 deep-research (보강 자료 조사)

- 상태: **done** (조사 종료, 1차 통합은 TASK-F 로 보류)
- 모드: Research (deep-research workflow)
- 우선순위: medium
- 담당: user (single-owner, deep-research 워크플로우 자동)
- 영향 산출물:
  - `Workflow({ name: "deep-research", args: ... })` 결과 (`/private/tmp/.../wxo7ehm8l.output`, 2588 lines)
  - 본 백로그 §8 (현재 섹션) — 결과 요약 + 1순위 + open questions

### 8.1 계획 (Plan)
- 작업 목표: html-slides-builder 보강을 위한 외부 레퍼런스 조사. 세 축:
  1. HTML 슬라이드 빌더 (reveal.js, Slidev, Marp, Spectacle, WebSlides)
  2. 디자인 시스템 (shadcn/ui, Radix, Material, Carbon, Tailwind UI, Headless UI)
  3. AI agent skill 생태계 (Claude Code, Cursor, Cline, Copilot)
- 사용자 결정: 세 축 모두 + 결론 형태 = 비교표 + 1순위 추천.
- 완료 기준:
  - [x] deep-research 워크플로우 5 phase 완료 (Scope / Search / Fetch / Verify / Synthesize)
  - [x] 15 confirmed / 10 refuted / 0 unverified (claim 25 → 15)
  - [x] 1순위 레퍼런스 선정 (Marpit)
  - [x] 4 open question 식별

### 8.2 실행 (Act)
- 진행 현황:
  - `2026-07-09 (세션8 이어서)` deep-research 워크플로우 launch (run `wf_58d963e2-c80`)
  - 5 phase 자동 진행: Scope (1 agent) → Search (5 agent) → Fetch (23 agent) → Verify (25×3=75 vote) → Synthesize
  - 일부 fetch agent 가 safety classifier 에 의해 block (4건), 1건 `MiniMax-M3[1m] classifier unavailable` warning — 결과 verified claims 와 무관
  - **15 confirmed 모두 Axis 1 (HTML 슬라이드 빌더)**. Axes 2/3 는 *uninvestigated* (verify 단계에서 kill, not investigated-empty)
  - 1순위 = **Marpit** (Marpit/Marp 생태계)
  - 4 open question:
    1. 디자인 시스템 중 우리 3-layer cascade 와 가장 정합한 것
    2. AI agent skills 의 명령 컨벤션 (anthropics/skills 의 pptx, frontend-design 등)
    3. Marpit section-ancestor scoping 의 hand-written .html 재현 가능성
    4. Marpit HTML-comment directive 의 모델-as-parser 적합성

### 8.3 검증 (Validate)
- 3-vote adversarial verification 통과 claim 15건
- 1순위 = Marpit (Marp-first markdown, `<section>` auto-scoping, build-step 없이 단일 .html 가능, MIT, active)
- Marpit `section` auto-scoping 의 *hand-written .html 재현* 은 inference — 테스트 필요
- Axes 2/3 의 claim 들은 verify 단계에서 kill 됨 — *1차 통합은 Marpit 패턴만* 으로 가능

### 8.4 결과 (Result)
- 작업 결과: TASK-PI 종료. 1순위 = Marpit.
- 다음 세션 시작 포인트: TASK-F (Marpit 통합) 시작 가능. Axes 2/3 는 후속 deep-research (2차) 별도 가능.

## 9. TASK-F Marpit 통합 (다음 세션)

- 상태: **pending (다음 세션)**
- 모드: Implementation
- 우선순위: medium
- 담당: user (single-owner)
- 영향 (예상):
  - `references/marpit-directives.md` (신설, ~50 줄) — Marpit-style `<!-- _directive: value -->` 디렉티브와 우리 3-layer cascade 의 매핑
  - `references/astryx-component-map.md` §2/§6 — `/* @theme name */` metadata + section auto-scoping 명문화
  - `scripts/verify_deck.py` — directive 검출 추가 (옵션, gate 2/3 확장)
  - `SKILL.md` — Two-tier config discipline (deck-level vs per-slide) 표준화 절차 추가

### 9.1 계획 (Plan)
- 작업 목표: Marpit 의 4가지 핵심 패턴을 html-slides-builder 에 통합:
  1. **Frontmatter + HTML-comment dual metadata carrier**
  2. **Three-scope directive system (global/local/spot, underscore prefix)**
  3. **`/* @theme name */` metadata + section-ancestor auto-scoping**
  4. **Two-tier config discipline (deck-level + per-slide overrides)**
- 보류 사유: 본 세션은 deep-research 결과 정리까지로 마무리. 사용자가 TASK-F 의 구체적 범위를 다음 세션에 결정.
- 완료 기준 (다음 세션):
  - [ ] 사용자 confirm: TASK-F 범위 (가벼운 통합 vs 전면 개편)
  - [ ] `references/marpit-directives.md` 신설 (또는 astryx-component-map.md 확장)
  - [ ] verify_deck.py gate 2/3 directive 검출 (옵션)
  - [ ] lint clean
  - [ ] 영향 문서 (CHANGELOG / handoff / work_backlog / state) 동기화
- 후속 옵션:
  - **옵션 A**: TASK-F 즉시 시작 (30-45분)
  - **옵션 B**: Axes 2/3 후속 deep-research (2차, 축 2개로 budget ↓) (10-15분)
  - **옵션 C**: 일단 보류 (사용자 직접 결정)
