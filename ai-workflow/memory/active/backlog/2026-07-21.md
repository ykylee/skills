<!-- standard-ai-workflow-kit: v0.13.3-beta -->

# 2026-07-21 작업 백로그

- 문서 목적: 당일의 구체적인 작업 계획과 실행 결과, 그리고 검증 내용을 기록한다.
- 범위: 당일 수행하는 모든 태스크의 상세 진행 상황
- 대상 독자: 개발자, AI 에이전트, 프로젝트 협업자
- 상태: done
- 최종 수정일: 2026-07-21
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md)

> 본 세션의 출발점은 "프로젝트 파악" 이었고, 파악 과정에서 드리프트가 연쇄적으로 발견되어
> TASK-I → TASK-L 로 이어졌다. 4 commit 모두 **미 push** 상태 (세션 종료 시점).

## 1. TASK-I 백로그 드리프트 정리

- 상태: **done** (commit `4accca3`)
- 모드: Operations
- 우선순위: high (규약 위반 상태)
- 담당: user (single-owner)
- 영향 문서:
  - `ai-workflow/memory/active/backlog/2026-07-14.md` (신규, 사후 보강)
  - `ai-workflow/memory/active/work_backlog.md` (§2 링크 + §3 요약 2줄)
  - `ai-workflow/memory/active/state.json` (`latest_backlog_path` ×2 + `next_documents` + `generated_at`)
  - `ai-workflow/memory/active/backlog/2026-07-09.md`, `2026-07-10.md` (상태 필드)

### 작업 내용

TASK-G / TASK-H 가 2026-07-14 에 완료되었으나 **날짜별 백로그 파일이 없었다**. 내용은
`state.json` 과 `session_handoff.md` 에 보존되어 정보 손실은 없었으나,
[PROJECT_PROFILE.md §4](../PROJECT_PROFILE.md) 의 "추가/삭제/이름변경은 `CHANGELOG.md` 와
`backlog/<date>.md` 에 동시 기록" 규약과 어긋난 상태였다.

1. `backlog/2026-07-14.md` 신규 — TASK-G (4 commit 의 원인/해소 개별 기록) + TASK-H
   (4 CI run 표, auto-classifier 제약, 학습된 3개 정책). 기존 백로그 형식 준수.
   문서 상단에 **사후 보강임을 명시하는 노트** 추가 — 2026-07-14 에 작성된 것처럼 보이면
   그 자체가 새 드리프트가 되기 때문.
2. `work_backlog.md` §2 에 날짜 링크, §3 요약에 TASK-G / TASK-H 2줄 추가 (§3 에도 누락돼 있었음).
3. `state.json` 의 `latest_backlog_path` 2곳 + `next_documents` 1곳을 `2026-07-14.md` 로 갱신.
4. `backlog/2026-07-09.md` / `2026-07-10.md` 의 헤더 `상태: in_progress` → `done`
   (두 파일 모두 전 항목 완료 상태였음).

### 검증

- `state.json` JSON 파싱 OK, `task_count: 23` == `done_items: 23`.
- `python3 scripts/skill-lint --path skills` — 5 SKILL.md clean.
- `markdownlint-cli2` (신규 + 수정 문서) — 0 issues.
- 상대 링크 3건 (`../work_backlog.md`, `../session_handoff.md`, `../PROJECT_PROFILE.md`) 실재 확인.

### 결과 / 결정

- CI 스코프 (`skills/**` + `README.md` + `CHANGELOG.md`) 밖이라 본 변경은 CI 대상 아님.
- **git identity 미설정 발견** — 본 머신의 git 에 `user.name=ssubb` 만 있고 email 부재.
  기존 커밋 38건은 전부 `ykylee <ddn777@hotmail.com>`. 사용자 확인 후 `--local` 로 설정
  (전역 설정은 건드리지 않음).

---

## 2. TASK-J 표준 YAML 파서 비호환 frontmatter 수정

- 상태: **done** (commit `e9cc089`)
- 모드: Implementation
- 우선순위: high (배포 중인 산출물이 깨진 상태)
- 담당: user (single-owner)
- 선행: 스킬 목록 확인 중 발견
- 영향 문서:
  - `skills/html-slides-builder/SKILL.md`, `skills/llm-wiki/SKILL.md`,
    `skills/react-premium-design/SKILL.md` (각 1줄)
  - `CHANGELOG.md` (`[Unreleased]` `### Fixed`)

### 발견 경위

"개발된 스킬 목록 확인" 요청으로 `scripts/skill-discover` 와 frontmatter 를 직접 파싱하던 중,
`when_to_use` 값의 앞뒤 따옴표가 잘려 보이는 것을 발견. 표준 YAML 파서로 교차 확인한 결과
**5건 중 3건이 파싱 자체 실패**.

pip 은 인덱스 접근 불가로 PyYAML 설치 실패 → npm 은 동작하므로 `npx js-yaml` 로 검증.

| 스킬 | 표준 YAML | 실패 위치 |
|---|---|---|
| `skill-lint` / `skill-discover` | OK | — |
| `html-slides-builder` | FAIL | `description` (2:105) |
| `llm-wiki` | FAIL | `when_to_use` (5:28) |
| `react-premium-design` | FAIL | `when_to_use` (5:36) |

### 원인 2종

- **평문 스칼라의 콜론+공백** (`html-slides-builder`) — `description: ... Triggers: 'a'` 에서
  `Triggers:` 가 두 번째 mapping key 로 해석됨. *작은따옴표는 원인이 아니었다* (초기 오진).
- **닫는 따옴표 뒤의 쉼표** (`llm-wiki`, `react-premium-design`) — `when_to_use: "A", "B"` 는
  `"A"` 에서 스칼라가 끝나므로 뒤가 문법 오류.

### 작업 내용

값은 그대로 두고 따옴표만 보정 (의미 변경 0):
`description` 은 `"` 로, `when_to_use` 2건은 `'` 로 감쌈 (내부 `"` 보존).

### 검증

- `npx js-yaml` — **5건 전부 OK**. js-yaml 은 첫 오류에서 멈추므로, 3건 수정 후 5개를 *다시
  전부 추출* 해 재검증 (뒤에 가려진 오류 확인).
- mini-parser ↔ js-yaml 값 대조 — description / when_to_use 10건 전부 일치.
- `skill-lint` / `--strict` clean, `skill-discover --index` 5 entries,
  `markdownlint` 18 files 0 issues.
- lychee 는 로컬 미설치로 미실행 (본 변경은 링크 무관).

### 결과 / 결정

- 이전에 mini-parser 가 잘라먹던 `when_to_use` 2건의 값이 **원래 의도대로 복원**됨.
- **핵심 인식**: `skill-lint` 가 clean 을 보고하고 CI 도 green 이었으나 실제로는 깨진 상태.
  검증 도구의 사각지대 → TASK-L 로 이어짐.

---

## 3. TASK-K 카탈로그 문서 드리프트 정리

- 상태: **done** (commit `e6f44cb`)
- 모드: Documentation
- 우선순위: medium
- 담당: user (single-owner)
- 영향 문서:
  - `README.md` (트리 1줄), `ai-workflow/wiki/index.md` (§4 1줄)
  - `skills/README.md` (§1.1 신규 / §3.1 예시 / §3.1.0 신규 / §3.1.3·§3.1.4 제목 / §6 체크리스트)
  - `CHANGELOG.md`

### 작업 내용

1. **`react-premium-design` 미등재 해소** — 저장소 `README.md` 트리와
   `ai-workflow/wiki/index.md` §4 에 누락. 5개 스킬 전부 두 문서에 등재됨.
2. **`skills/README.md` §3.1 frontmatter 예시가 stale** — 초기 진단 ("목록 2:5 누락") 은
   *오진* 이었고, 열어보니 해당 문서는 목록이 아니라 **authoring 가이드**. 대신 더 심각한
   문제 발견: 예시가 2026-07-10 정책 *이전* 형태 (top-level `when_to_use` +
   `metadata.harness_compat`) 를 가르치고 있었다. 실제는 `metadata.claude_code.*` depth-2
   nested 이고 `skill-lint` E040 이 그 경로를 읽으므로, **가이드대로 쓰면 lint 실패**.
   예시 + §3.1.3 / §3.1.4 제목 + §6 체크리스트 보정.
3. **§3.1.0 (YAML 인용 규칙) 신설** — TASK-J 의 실패 유형 2종 + `js-yaml` 확인 절차.
4. **§1.1 신설** — 스킬 목록을 손으로 복사하는 대신 `scripts/skill-discover` 로 생성하도록
   안내. **목록을 추가하지 않은 것이 의도** — 손 관리 목록 3곳이 어긋나서 생긴 문제인데
   4번째를 만들면 반복된다. 사람이 읽는 트리는 루트 README 하나로 유지.

### 검증

- 문서에 넣은 `js-yaml` 명령을 *실제로 실행*. 처음 쓴 프로세스 치환 형태 (`<(...)`) 는
  js-yaml 이 `/proc/…/fd/…` 를 읽지 못해 실패 → 임시 파일 방식으로 교체 후,
  정상 SKILL.md exit 0 / 고장 케이스 non-zero 까지 확인하고 문서화.
- `markdownlint` 18 files 0 issues, `wiki/index.md` clean, `skill-lint --strict` clean.
- 5개 스킬이 두 문서에 모두 등재됐는지 grep 으로 확인.

---

## 4. TASK-L skill-lint E002 표준 YAML 교차검증

- 상태: **done** (commit `0cbeb31`)
- 모드: Implementation
- 우선순위: high (TASK-J 재발 방지)
- 담당: user (single-owner)
- 선행: TASK-J (검증 도구 사각지대 확인)
- 영향 문서:
  - `scripts/_frontmatter.py` (`check_yaml_strict()` 신규 + `_strip_quotes()` 버그 수정)
  - `scripts/skill-lint` (E002 배선 + docstring)
  - `skills/skill-lint/SKILL.md` (§Procedure 단계 + rule 목록 + E040 경로 보정)
  - `skills/README.md` §3.1.0 (자동 검출로 안내 변경)
  - `CHANGELOG.md` `### Infra`

### 설계 결정 — PyYAML 도입하지 않음

`stdlib-only` 는 PROJECT_PROFILE §3 / README / `_frontmatter.py` docstring 에 반복 명시된
속성이고, 본 환경에서는 pip 인덱스 접근도 불가. 따라서 **mini-parser 의 관대함이 값을
조용히 망가뜨리는 구조만** 골라 stdlib 로 검출하는 방향 채택.

CI 에 `npx js-yaml` 단계를 추가하는 안도 있었으나, **CI workflow 수정은 사전 사용자 승인
필요** (2026-07-14 학습 제약) 이므로 본 세션에서는 진행하지 않음.

### 작업 내용

1. **`check_yaml_strict()` 신규** — 검출 3종: 평문 스칼라의 콜론+공백 / 닫는 따옴표 뒤
   잔여 내용 / 미닫힘 따옴표. 파일 기준 줄 번호 보고. double-quote 의 `\` escape 와
   single-quote 의 `''` 리터럴 처리 포함.
2. **`_strip_quotes()` 버그 수정** (별건 발견) — `.strip('"').strip("'")` 이 따옴표를 개수
   제한 없이 벗겨내어 `say 'hi'` → `say 'hi` 로 끝 따옴표를 잘라먹었다. *짝이 맞는 한 쌍*
   만 제거하도록 변경.
3. `skill-lint` 에 E002 배선 (`check_file` 에서 호출, 줄 번호 포함).

### 검증 — js-yaml 차등 테스트

검사기의 정확성 기준 = "실제 파서와 같은 판정을 내리는가" 이므로 그렇게 측정.

- **16 케이스 (정상 8 / 깨짐 8) 전부 일치, false positive 0, 미검출 0.**
  정상 통과 확인: URL 값 (`https://a:b/c`), 평문 아포스트로피 (`don't`), `''` / `\"` escape,
  inline list, 뒤따르는 주석.
- 실제 5 SKILL.md 를 **전체 키 재귀 비교** — js-yaml 과 불일치 0
  (`_strip_quotes` 변경이 값 보존적임을 확인).
- 원래 버그 2종을 재현한 픽스처 스킬 → exit 1 검출, 줄 번호 정확 (초기 off-by-one 발견 후 수정).
- 임시 픽스처 삭제 확인 (작업 트리 잔재 0).

### 한계 (문서화됨)

전체 YAML 스펙 검증이 **아니다**. multi-line scalar / anchor / flow mapping 은 대상 밖이며,
16 케이스도 자체 설계 corpus 이므로 "미검출 0" 은 그 범위 안의 이야기.
`skills/README.md` §3.1.0 에 복잡한 frontmatter 용 js-yaml 직접 확인 절차를 함께 유지.

### 부수 발견

`skills/skill-lint/SKILL.md` 의 E040 도 `metadata.harness_compat` 로 stale (TASK-K 에서
authoring 가이드에 고친 것과 동일 경로). 함께 보정하고 §Procedure 단계 renumber.

---

## 5. 세션 산출물 요약

| commit | 내용 |
|---|---|
| `4accca3` | TASK-I 백로그 드리프트 (2026-07-14 백로그 사후 보강) |
| `e9cc089` | TASK-J YAML frontmatter 3건 |
| `e6f44cb` | TASK-K 카탈로그 문서 (미등재 + stale 가이드) |
| `0cbeb31` | TASK-L E002 표준 YAML 교차검증 |

### Out of Scope (다음 세션 task 후보)

- **push 미수행** — 4 commit 모두 로컬. `skills/**` 를 건드리므로 push 시 CI 가 돈다.
- **CI 에 표준 YAML 단계 추가** — E002 가 stdlib 범위만 보므로, `npx js-yaml` 을 CI 에
  넣으면 전체 스펙 검증이 된다. **CI workflow 수정 = 사전 사용자 승인 필요**.
- **lychee 로컬 미설치** — 본 세션 변경은 링크 무관이라 영향 없으나, 로컬 3-step 검증이
  2-step 으로만 가능한 상태.
- **`skill-lint` 의 cwd 제약** — `--path` 가 cwd 하위가 아니면 `ValueError`
  (`path.relative_to(repo_root)`). 저장소 밖 디렉터리 검사 불가. 사전 존재 한계, 미수정.
- **D. llm-wiki 실전 검증** / **A. UnoCSS FOUC** / **B. UnoCSS preset** — 이전 세션부터 이월.
