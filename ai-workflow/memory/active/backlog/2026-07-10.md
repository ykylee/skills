<!-- standard-ai-workflow-kit: v0.13.3-beta -->

# 2026-07-10 작업 백로그

- 문서 목적: 당일의 구체적인 작업 계획과 실행 결과, 그리고 검증 내용을 기록한다.
- 범위: 당일 수행하는 모든 태스크의 상세 진행 상황
- 대상 독자: 개발자, AI 에이전트, 프로젝트 협업자
- 상태: in_progress
- 최종 수정일: 2026-07-10
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md)

## 1. TASK-F Marpit 통합

- 상태: **done**
- 모드: Design | Implementation
- 우선순위: high
- 담당: user (single-owner)
- 선행: TASK-PI 외부 레퍼런스 deep-research (Marpit 1순위, 2026-07-09)
- 영향 문서:
  - `skills/html-slides-builder/references/marpit-directives.md` (신규, ~160줄)
  - `skills/html-slides-builder/references/astryx-component-map.md` (§2 분할, +33줄)
  - `skills/html-slides-builder/SKILL.md` (§Procedure Phase 1 step 6 보강 + §References 추가, +3줄)
  - `skills/html-slides-builder/scripts/verify_deck.py` (`--strict` 옵션 + gate1 확장, +14줄)
  - `CHANGELOG.md` (`[Unreleased]` Added / Changed / Fixed 항목 추가 + MD024 해소)
  - `ai-workflow/memory/active/session_handoff.md` (TASK-F done 표시 + Key Changes + Next Actions 갱신)
  - `ai-workflow/memory/active/work_backlog.md` (TASK-F [x] + 2026-07-10 백로그 link 추가)
  - `ai-workflow/memory/active/state.json` (recent_done_items + next_documents 갱신)

### 작업 내용

1. **선행 조사**: `astryx-component-map.md` §2 + `verify_deck.py` 전체 + `SKILL.md` Procedure
   Phase 1 step 6 부근 확인. 기존 메타데이터 컨벤션(`/* mood: */`) 의 위치와 검증 패턴 파악.
2. **`references/marpit-directives.md` 신설** — Marpit 4가지 핵심 패턴 (dual metadata carrier,
   three-scope directive, `@theme` metadata + section auto-scoping, two-tier config) 을 우리
   hand-written no-build deck 에 매핑. §0 Read this when / NOT when, §1 Why Marpit patterns,
   §2 Three-scope (global / local / spot), §3 3-layer CSS cascade 와의 매핑 표, §4 canonical
   metadata comment 규약, §5 사용 예시, §6 anti-patterns, §7 See also + Marpit docs link.
3. **`references/astryx-component-map.md` §2 분할** — 기존 §2 본문을 §2.1 Layer structure 로,
   §2.2 Metadata convention (NEW, `/* @theme: */` + `/* mood: */` 규약) 추가, §2.3 Section
   scoping (NEW, hand-written 재현 명시, `:where([data-slide-root])` prefix 의도적 skip) 추가.
   §6 끝에 marpit-directives.md §2.2 + §4 참조 1줄.
4. **`SKILL.md` §Procedure Phase 1 step 6 보강** — 기존 step 6 끝에 metadata comment 규약
   1 paragraph (verify_deck.py `--strict` + marpit-directives.md §2–§4 참조).
5. **`SKILL.md` §References 갱신** — `marpit-directives.md` 항목 추가, `verify_deck.py` 항목에
   `--strict` 옵션 명시.
6. **`scripts/verify_deck.py` `--strict` 옵션 추가** — `gate1(path, strict=False)` signature
   변경, `/* @theme: */` 와 `/* mood: */` 부재 시 WARN (FAIL 아님). argparse 에 `--strict` 옵션
   추가, main 의 gate1 호출에 `strict=args.strict` 전달.
7. **기존 MD024 + MD029 즉시 해소** (사용자 결정):
   - `CHANGELOG.md` 의 두 번째 `### Changed` (TASK-002-B 운영 자동화 항목) → `### Infra` 로 분리
     (MD024 siblings_only 해소).
   - `SKILL.md` §Procedure Phase 2/3/4 의 step 번호 7~13 → 각 Phase 별 1~N 으로 renumber
     (MD029 ol-prefix 해소). 본문에서 step 번호 직접 참조 없음 확인.

### 검증

- `python3 scripts/skill-lint --path skills` — 3 SKILL.md clean.
- `npx --yes markdownlint-cli2 "skills/**/*.md" "README.md" "CHANGELOG.md"` — 0 error
  (마릿말 8 → 0).
- `python3 scripts/verify_deck.py --help` — `--strict` 옵션 노출 확인.
- `python3 scripts/verify_deck.py /tmp/_test_strict.html --gate 1 --strict` — metadata comment
  없을 때 WARN 출력 + exit 0 확인.
- `python3 scripts/verify_deck.py /tmp/_test_strict_ok.html --gate 1 --strict` — metadata comment
  있을 때 WARN 없음 + exit 0 확인.

### 결과 / 결정

- TASK-F Full scope (문서 3건 + verify 1건) 모두 완료.
- 기존 lint error 즉시 해소 채택 (사용자 결정). 다음 세션 잔여 항목은 없음.
- 빌더 자동화는 *본 세션 scope 외*. verify_deck.py 의 `--strict` 는 metadata comment 부재 시
  WARN 만, build-step 도입은 별도 task.

---

## 2. ai-workflow/wiki/index.md 신규

- 상태: **done**
- 모드: Implementation
- 우선순위: low (TASK-F 보강)
- 담당: user (single-owner)
- 영향 문서:
  - `ai-workflow/wiki/index.md` (신규, ~75줄, R4 anchor 형식)
  - `ai-workflow/memory/active/session_handoff.md` (wiki 항목 추가 + Next Actions 갱신)
  - `ai-workflow/memory/active/state.json` (next_documents + recent_done_items 갱신)
  - `ai-workflow/memory/active/work_backlog.md` (wiki [x] 추가)

### 작업 내용

CLAUDE.md 의 "다음에 읽을 문서" 에 명시되어 있으나 부재한 `ai-workflow/wiki/index.md` 신규.
R4 anchor 형식 (`<a id="anchor-X"></a>`) 사용. 사용자 선택: 운영 도구 + 레퍼런스 link 까지
포함.

§1 Project Overview / §2 State / §3 Active Operations (운영 도구 + 표준 문서) / §4 Skill
Catalog (도메인 + 메타) / §5 In-Session Slash Commands / §6 How to read this wiki. 각 절에
anchor + 문서 link, brief description.

### 검증

- `python3 -c "from pathlib import Path; p = Path('ai-workflow/wiki/index.md'); print(p.stat().st_size, 'bytes')"` — 75줄.
- 기존 wiki 부재 해소 (CLAUDE.md 의 "다음에 읽을 문서" 정합).
- 위키 자체는 frontmatter 가 없고 reference 가 아니므로 skill-lint / markdownlint-cli2 의
  검증 대상 외 (CI 자체 영향 없음).
- `git diff --stat` 으로 신규 파일 1건만 추가 확인 예정.

### 결과

CLAUDE.md 의 "다음에 읽을 문서" 정합 회복. 향후 신규 AI 에이전트는 §2 (state + handoff +
backlog) 를 *먼저* 로드하여 컨텍스트를 복원. state.json 의 `next_documents` 에
`ai-workflow/wiki/index.md` 추가됨.
